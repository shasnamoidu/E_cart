//List of enums
//they do not create inside a class

enum TextSizes {small, medium, large}

enum OrderStatus {Processing, shipped, delivered}

enum PaymentMethod {googlepay, applepay, visa, creditCard, paytm}